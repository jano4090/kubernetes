---
all:
  children:
    kubernetes_masters:
      hosts:
        fedora-master:
          ansible_host: 192.168.1.69  # Replace with your Fedora VM IP
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
    kubernetes_workers:
      hosts: {}  # For single node cluster, we'll use master as worker too